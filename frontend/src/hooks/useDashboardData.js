import {
  activities,
  campaignRows,
  headerData,
  menuSections,
  metricCards,
  revenueSources,
} from '../data/dashboardData'

function useDashboardData() {
  return {
    activities,
    campaignRows,
    headerData,
    menuSections,
    metricCards,
    revenueSources,
  }
}

export default useDashboardData
